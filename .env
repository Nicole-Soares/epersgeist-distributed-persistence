# Configuración por defecto de perfiles de Docker Compose
# Al definir COMPOSE_PROFILES, 'docker compose up -d' levantará todos los servicios automáticamente sin requerir banderas.

COMPOSE_PROFILES=kafka,sql,nosql,graph,app,micro,front
